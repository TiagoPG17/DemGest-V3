APP_NAME=DemGest
APP_ENV=production
APP_KEY=base64:hZIyFL5gnYRlz7fyrPI3OVa3Oz8PMRhFbqTHaAV9VfY=
APP_DEBUG=false
APP_URL=https://tu-dominio.com  # CAMBIAR POR TU DOMINIO REAL
ASSET_URL=https://tu-dominio.com  # CAMBIAR POR TU DOMINIO REAL

# =====================================================================
# CONFIGURACIÓN PARA ACCESO MÓVIL EN PRODUCCIÓN
# =====================================================================
# En producción, las URLs usan automáticamente el dominio real
# No se necesita reemplazo de URLs como en desarrollo
MOBILE_ENABLE_URL_REPLACEMENT=false

# Configuración del servidor para acceso móvil
MOBILE_SERVER_IP=tu-ip-servidor  # CAMBIAR POR TU IP REAL
MOBILE_SERVER_PORT=443
MOBILE_PROTOCOL=https

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error  # Solo errores en producción
LOG_STDERR_FORMATTER=Monolog\Formatter\JsonFormatter

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=demgest
DB_USERNAME=laravel
DB_PASSWORD=secret

BROADCAST_DRIVER=log
CACHE_DRIVER=redis  # Mejor rendimiento para caché
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis  # Mejor para colas en producción
SESSION_DRIVER=redis  # Mejor rendimiento para sesiones
SESSION_LIFETIME=120

# Configuración de caché optimizada
CACHE_PREFIX=demgest_
CACHE_REDIS_CONNECTION=default
CACHE_REDIS_CACHE_DB=1

# Configuración de sesión optimizada
SESSION_REDIS_CONNECTION=default
SESSION_REDIS_SESSION_DB=0

MEMCACHED_HOST=127.0.0.1

# Configuración de Redis optimizada para producción
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CLIENT=phpredis
REDIS_CLUSTER=redis
REDIS_PREFIX=demgest_

# Configuración de conexión a base de datos optimizada
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=demgest
DB_USERNAME=tu_usuario_db  # CAMBIAR POR TU USUARIO REAL
DB_PASSWORD=tu_password_db  # CAMBIAR POR TU PASSWORD REAL

# Opciones de base de datos optimizadas
DB_OPTIONS="--max_allowed_packet=64M --default_time_zone='+00:00'"

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
